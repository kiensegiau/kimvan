import AuthLayout from '../auth/layout';

/**
 * Layout cho trang quên mật khẩu sử dụng layout xác thực chung
 */
export default function ForgotPasswordLayout({ children }) {
  return <AuthLayout>{children}</AuthLayout>;
} 